type DidDocument @entity {
  id: String!
  controller: String!
  nameToDidDocument: NameToDidDocument! @derivedFrom(field: "didDocument")
}

type NameToDidDocument @entity {
  id: String!
  didDocument: DidDocument!
}

